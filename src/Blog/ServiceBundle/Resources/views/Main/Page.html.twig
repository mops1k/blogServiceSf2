{# author: mops1k #}
{% extends 'BlogServiceBundle:Layout:base.html.twig' %}

{% block content %}
    <div class="page-header">
        <h1>{{ page.title }}</h1>
    </div>
    <p>{{ page.content|raw }}</p>
{% endblock %}
